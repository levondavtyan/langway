<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:fillViewport="true"
    android:clipToPadding="false">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="4dp"
        android:paddingEnd="4dp"
        android:paddingTop="4dp"
        android:paddingBottom="32dp"
        android:clipToPadding="false">

        <!-- Subtitle -->
        <TextView
            android:id="@+id/hobbies_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:layout_marginBottom="16dp"
            android:text="Pick the topics you enjoy — your matches will share them"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            android:alpha="0" />

        <!-- Hobby chips grid — wrapped in a card for the same look as the picker card -->
        <androidx.cardview.widget.CardView
            android:id="@+id/hobbies_card"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="2dp"
            android:layout_marginEnd="2dp"
            app:cardBackgroundColor="@color/surface"
            app:cardCornerRadius="@dimen/card_corner_radius"
            app:cardElevation="6dp"
            app:cardUseCompatPadding="true">

            <!--
                ChipGroup with app:chipSpacing so chips wrap and breathe.
                Chips are added programmatically in HobbiesFragment.
            -->
            <com.google.android.material.chip.ChipGroup
                android:id="@+id/chip_group"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp"
                app:chipSpacing="8dp"
                app:singleLine="false" />

        </androidx.cardview.widget.CardView>

        <!-- Selection count feedback -->
        <TextView
            android:id="@+id/selection_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:layout_marginStart="6dp"
            android:text="Nothing selected yet"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            android:alpha="0" />

    </LinearLayout>
</androidx.core.widget.NestedScrollView>